DATABASE_URL=postgresql://user:password@localhost:5432/option_strategy
OPTION_SAMURAI_EMAIL=your_email@example.com
OPTION_SAMURAI_PASSWORD=your_password
MASSIVE_API_KEY=your_polygon_option_api_key
MASSIVE_STOCK_API_KEY=your_polygon_stock_api_key
PORT=3000
NODE_ENV=development
DATA_DIR=/data

# ── Authentication (required in production, optional in dev) ──
# Shared secret with Member Portal (must match portal's OPTION_STRATEGY_TOKEN_SECRET)
PREMIUM_TOKEN_SECRET=
# Local session signing key (unique to this service, generate with: openssl rand -base64 32)
JWT_SECRET=
# Member Portal URL (used for CORS origin and unauthenticated redirects)
MEMBER_PORTAL_URL=https://portal.cyclescope.com

# ── Frontend Auth ──
VITE_MEMBER_PORTAL_URL=https://portal.cyclescope.com
